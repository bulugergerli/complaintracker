import { Container } from "@mui/material";
import React from "react";

const HomePage: React.FC = () => {
  return <Container maxWidth="sm">ComplainTracker | Homepage</Container>;
};

export default HomePage;
